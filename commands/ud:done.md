# Task Done

If `.claude/docs/INSTRUCTIONS.md` does not exist, warn the user and suggest running `/ud:init` instead of proceeding.

Follow the **Completing a Task** protocol in `.claude/docs/INSTRUCTIONS.md`.
